# icepush-v1 — 2026-07-16

radio-resample-v1 + icecast SOURCE-client push: /ice/start|stop|state +
web PUSH block stream the output as 96k mono MP3 to an icecast mountpoint
(config persisted in usr/ICECAST.JSN). Verified against tools/mock_icecast.py.
Also: web remote v2-v5 polish, ota.sh loud-abort. `./flash.sh [PORT]` = recovery.
