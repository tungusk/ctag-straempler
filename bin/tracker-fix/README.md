# tracker-fix — 2026-07-17

tape-v1 + the tracker resurrection (checked render-task stack ladder, 4K
load stage, lazy shadow FB) and the full docs/machines set. Verified: 8ch IT
module loads + plays, autoload on boot works. flash.sh = serial recovery.
