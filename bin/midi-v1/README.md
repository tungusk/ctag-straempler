# midi-v1 — 2026-07-17

tracker-fix + the web MIDI bridge: PLAY-MIDI card (musical typing, on-screen
keys, WebMIDI capture) -> /ws/midi -> soft-MIDI note stack -> Synth/Keys
pitch+gate. Verified: note 69 = 440.0 Hz bounced. flash.sh = serial recovery.
