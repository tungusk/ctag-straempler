#ifndef MACHINE_TAPE_H
#define MACHINE_TAPE_H

#include "machine.h"

// "Tape" — single-track tape recorder/editor (big-waveform UI, crop + grid,
// FX chain in the record path, cut/copy/paste). Registry-only symbol.
extern const machine_t machine_tape;

#endif
